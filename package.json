{
  "name": "mini4wd-layout-editor",
  "version": "1.1.0-rc.6",
  "private": true,
  "description": "Tooling path for typed Mini 4WD layout domain modules while preserving the current static browser runtime.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --pretty false",
    "lint": "tsc -p tsconfig.json --noEmit --pretty false --noUnusedLocals --noUnusedParameters --noFallthroughCasesInSwitch --noImplicitReturns",
    "build": "tsc -p tsconfig.build.json --pretty false",
    "check:typed": "npm run typecheck && npm run lint && npm run build"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=22"
  }
}
