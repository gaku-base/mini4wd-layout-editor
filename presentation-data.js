'use strict';

// Pure presentation data helpers live here so they can be unit-tested without touching editor state.
(function registerPresentationData(global) {
  global.M4WD_PRESENTATION_DATA = Object.freeze({ version: 1 });
})(window);
