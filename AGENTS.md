# AGENTS.md

このリポジトリで作業するすべてのCodexエージェントは、本書を最優先の開発ルールとして扱うこと。

## プロジェクトの目的

PC・タブレットで実用できる、2D中心のミニ四駆コースレイアウトエディターを開発する。
画面は2Dだが、内部ではmm単位のXYZ座標と実寸3D形状を保持する「2.5D設計アプリ」とする。

## 確定済みの絶対条件

- 内部単位はすべてmmとする。
- ストレート1枚の長さは540mmとする。
- スロープ1枚の高低差は115mmとする。
- 上記以外の未確認寸法を、推測で確定値として実装してはならない。
- 寸法には `verified` / `provisional` / `unknown` の状態と根拠を持たせる。
- UIは2D中心とし、内部座標はX・Y・Zを保持する。
- 干渉判定の対象はコースパーツ本体同士だけとする。
- 支柱、橋脚、補強材、固定具、テープ、会場床は干渉判定に含めない。
- スロープは単純な直方体や入口・出口2点だけで確定してはならない。
- スロープ下面には、下にコースを通せる部分と通せない部分がある。実寸3D形状を基にした衝突形状へ差し替え可能な設計にする。
- 正式な接続コネクタ同士の接触は、パーツ干渉として誤検出しない。
- NOIRサイトのコード、画像、3Dモデル、ロゴ、素材をコピーしない。寸法・形状を把握するための参考にとどめ、表示形状と衝突形状は独自に作る。

## 基本操作の絶対条件

- 数字キーでパーツを選択する。
- 選択したパーツは半透明シャドーとしてマウスポインターに追従する。
- シャドーの初期角度は、アクティブな直前パーツの出口角度に合わせる。
- 初期角度は候補にすぎず、ホイールまたはZ/Xで回転した場合は手動角度を優先する。
- ホイール回転は45度刻みとする。
- クリックで直前パーツの接続点へ吸着して配置する。
- 参考操作感は https://mini4wd-track-editor.pimentoso.com/ とするが、コードや素材はコピーしない。

## 編集ルール

- パーツ1個を変更しても、変更対象外のコースを自動移動させない。
- 複数の連結パーツを移動しても、選択範囲外のコースを自動移動させない。
- 接続が外れた場合は、未接続点を表示し、既存部分は固定する。
- 連結区間は、走行方向の起点→終点と終点→起点の両方で選択可能にする。
- 選択した連結区間は、独立した仮置きグループとして移動・回転・保存できる構造にする。

## 接続補助ルール

- 未接続端点同士がつながりそうな場合、接続可能なパーツ構成を実パーツとは別のシャドーレイヤーで提示する。
- 作成者が候補の一部を配置した場合、残り候補を追従更新する。
- 作成者が候補と異なるパーツを配置した場合、現在状態から再探索する。
- シャドー候補を勝手に確定しない。一括採用の明示操作がある場合だけ実パーツ化する。

## 干渉判定ルール

- XY上で重なる可能性があるパーツだけ、詳細3D判定へ進める。
- 正常な上下交差はエラーにしない。
- 同じ高さの重なり、スロープ下面との接触、側壁同士の接触、接続関係のないパーツの食い込みをエラーとする。
- 干渉時は対象パーツと干渉範囲を赤く視覚表示できる情報を返す。
- 移動中は軽量判定、配置確定時は厳密判定を行える二段階構成にする。

## 実装方針

- TypeScriptを使用し、計算ロジックとUIを分離する。
- 接続・座標・高さ・干渉計算は純粋関数を優先する。
- 未確認値をマジックナンバーとして埋め込まない。
- 新しい仕様判断を勝手に追加しない。未決事項はTODOまたはIssueとして明示する。
- 変更範囲はタスクに必要な最小限とする。
- 未使用素材、重複ファイル、旧バックアップ、中間生成物をリポジトリへ追加しない。
- 既存仕様を変更する場合は `docs/decision-log.md` と関連仕様書を同時に更新する。

## テスト要件

- 機能追加・修正には、対応する自動テストを追加する。
- 最低限、型チェック、Lint、単体テスト、ビルドを通す。
- 接続計算では、ストレート3枚が1620mmになることを検証する。
- スロープ接続後に出口高さが115mm上昇することを検証する。
- 45度回転後も入口・出口座標が正しいことを検証する。
- 100～200パーツの連続配置で許容できない座標誤差が蓄積しないことを検証する。
- 仕様上の暫定モデルを使用するテストは、暫定であることをテスト名とコメントに明記する。

## PRに必ず記載する内容

- 実装内容
- 変更ファイル
- 実行したテストと結果
- 使用した確定値・暫定値
- 未解決事項
- 仕様書を更新したか
- 次の推奨タスク
